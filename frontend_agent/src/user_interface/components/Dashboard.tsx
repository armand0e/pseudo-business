import type { FC } from 'react';

const Dashboard: FC = () => {
    return (
        <div>
            <h2>Dashboard</h2>
            <p>Real-time project progress will be displayed here.</p>
        </div>
    );
};

export default Dashboard;